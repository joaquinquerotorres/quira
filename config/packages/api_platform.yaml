api_platform:
    title: Quira API
    version: 1.0.0
    formats:
        jsonld: ['application/ld+json']
        json: ['application/json']
    eager_loading:
        max_joins: 60 
        force_eager: false
    defaults:
        stateless: true
        cache_headers:
            vary: ['Content-Type', 'Authorization', 'Origin']
    swagger:
        api_keys:
            JWT:
                name: Authorization
                type: header
